<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.UserDao">

    <select id="findById" resultType="com.example.demo.model.User">
        select ID_ as id,CONCAT(FIRST_,LAST_) as name,EMAIL_ as email,PWD_ as password from act_id_user
        where id = #{id}
    </select>

    <select id="getUser" resultType="com.example.demo.model.User">
        select ID_ as id,CONCAT(FIRST_,LAST_) as name,EMAIL_ as email,PWD_ as password from act_id_user
        where EMAIL_ = #{email} and  PWD_ = #{password}
    </select>

</mapper>